{
  "name": "stencil-sandbox",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "stencil build",
    "start": "stencil build --dev --watch --serve"
  },
  "devDependencies": {
    "@stencil/core": "^2.5.2"
  }
}
